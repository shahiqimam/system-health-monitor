{
  "extends": "./tsconfig.json",
  "comment": "nest build uses this file. Excluding tests keeps rootDir at src/, so the entry point stays dist/main.js rather than dist/src/main.js.",
  "exclude": ["node_modules", "test", "dist", "**/*.spec.ts", "**/*.e2e-spec.ts"]
}
